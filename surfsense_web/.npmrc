# Allow installation with peer dependency mismatches
# Required for packages that haven't updated peer dependencies for React 19 yet
# (e.g., emblor@1.4.8 requires react@^18.0.0 but we use react@19.x)
legacy-peer-deps=true
